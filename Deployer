pipeline {
    agent any

    // Tools to be installed

    stages {
        stage('Collect') {
            steps {
                // Get code from GH repo
//                 git 'git remote add origin https://github.com/luisitsilva/sheller.git'
                // sh "sh start.sh"
            }
        }
    }
    post {
        always {
            // always do this
            sh "ls -al"
        }
    }
}
